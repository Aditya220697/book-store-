<h1><%= @category.name %> Books</h1>

<ul>
  <% @books.each do |book| %>
    <li>
      <%= link_to book.title, book %>
      by <%= book.author.name %>
      (Price: <%= number_to_currency(book.price) %>)
    </li>
  <% end %>
</ul>
